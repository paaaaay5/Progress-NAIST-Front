input AMPLIFY { globalAuthRule: AuthRule = { allow: public } } # FOR TESTING ONLY!

type Todo @model {
  id: ID!
  name: String!
  getLevels : [Levels]
  image: String!
}

type Levels {
  hackthon : Int!
  naist : Int!
}